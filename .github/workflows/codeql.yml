name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 0 * * 1"

jobs:
  analyze:
    uses: thedavidweng/cli-workflow-template/.github/workflows/go-codeql.yml@main
